# Placeholder only. Do not put a real app password in git.
# Create the live secret from .env:
#   kubectl create secret generic huntmail-smtp -n huntmail --from-env-file=.env --dry-run=client -o yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: huntmail-smtp
  namespace: huntmail
  labels:
    app: huntmail
type: Opaque
stringData:
  SMTP_EMAIL: sourabhdey21@gmail.com
  SMTP_APP_PASSWORD: nemb plsd bknh rsbd
  SENDER_NAME: Sourabh Dey
